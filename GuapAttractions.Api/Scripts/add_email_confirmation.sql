-- Выполните один раз на базе guap_routes_db (если столбец ещё не добавлен)
IF COL_LENGTH('dbo.пользователь', 'подтверждение_почты') IS NULL
    ALTER TABLE [dbo].[пользователь]
    ADD [подтверждение_почты] BIT NOT NULL
        CONSTRAINT DF_подтверждение_почты DEFAULT 1;
-- Существующие строки получают 1 (уже зарегистрированные считаются подтверждёнными)
